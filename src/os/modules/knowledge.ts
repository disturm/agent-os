/**
 * Модуль «Справка»: вопрос «как это работает», а не «составь мне план».
 *
 * Набор самый узкий: база знаний плюс профиль, чтобы ответ был про этого человека, а не
 * вообще. Дневник, погода и калькуляторы сюда не входят — на объяснение они не влияют.
 * Формат ответа при этом остаётся планом: он один на весь проект, и ревьюер проверяет его.
 */

import type { OsModule } from './index';

export const knowledge: OsModule = {
  name: 'knowledge',
  description:
    'Объяснение и справка по базе знаний: как считается норма, чем заменить продукт, почему так, а не иначе. ' +
    'Сюда идут «как считать белок», «зачем разминка», «в чём разница между этими подходами».',
  promptFile: 'knowledge.md',
  tools: ['read_profile', 'searchKnowledge'],
};
