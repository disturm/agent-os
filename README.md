# Wellness-агент

Next.js-приложение с одним экраном: коуч по питанию, тренировкам, восстановлению и привычкам с обязательной проверкой безопасности.

Это **не медицинский продукт**. Диагнозы, лекарства, анализы и лечение — вне зоны ответственности: на такие запросы агент останавливается и отправляет к специалисту.

## Установка

```bash
npm install
```

Нужен Node.js 20+ (проверено на 24).

## Ключ DeepSeek

Скопируйте `.env.example` в `.env` и подставьте свой ключ с https://platform.deepseek.com/api_keys:

```
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
```

`DEEPSEEK_BASE_URL` и `DEEPSEEK_MODEL` необязательны: по умолчанию `https://api.deepseek.com` и `deepseek-v4-pro`. `deepseek-v4-flash` дешевле и быстрее, для этой задачи хватает.

## Запуск

```bash
npm run dev
```

Откройте http://localhost:3000, введите задачу («составь план питания на завтра») и нажмите **Запустить агента** или `Ctrl`+`Enter`. Пока идёт цикл, на месте плана стоит скелетон — стриминга нет, ответ приходит целиком. Один запуск = одна задача, состояние между запросами не хранится.

Экран в две колонки: слева ввод и итог safety review (вердикт, score, число раундов, свёрнутая история раундов с пометкой итогового, замечания, время прогона и версии промптов), справа — финальный план. Одобренный план дополнительно пишется в `data/output.md`. Лог каждого раунда идёт в консоль сервера.

Проверить предохранитель: введите «подбери мне лекарство от давления». Ожидаемо: заметное предупреждение «Этот запрос требует консультации специалиста», `verdict=needs_human_professional`, план не показывается, `data/output.md` не перезаписывается.

Тот же цикл доступен из терминала:

```bash
npm run agent -- "составь план питания на завтра"
```

## Как это устроено

1. Задача уходит POST-запросом на `/api/agent/run` в виде `{ task }`.
2. Роут вызывает `runHealthAgent(task)` и возвращает `{ plan, review, rounds, finalRound, finalScore, improved, promptVersions, durationMs }`.
3. `data/profile.md` и `data/log.md` целиком подставляются в контекст, системные промпты берутся из `prompts/` по активной версии.
4. **Health Coach** генерирует план.
5. **Safety Reviewer** проверяет его по трём критериям (безопасность, реалистичность, соответствие профилю) и отвечает строго JSON — валидация через Zod, на невалидный ответ один ретрай.
6. `revise` → issues уходят обратно коучу. `needs_human_professional` → остановка. `approve` → запись в `data/output.md`.
7. Раундов минимум 2 и максимум 3 (`runHealthAgent(task, { minRounds, maxRounds })`). Минимум нужен, чтобы коуч гарантированно получил обратную связь: одобрение в первом раунде цикл не завершает.
8. Каждый раунд остаётся в истории: `rounds[]` с планом и вердиктом, поверх неё считаются `finalRound`, `finalScore` и `improved`.

Оркестрация — в коде, а не в промпте. Инструментов и function calling у агентов нет: они работают только на контексте из файлов.

Вокруг агентов — harness, слой контроля: он подставляет промпт нужной версии, проверяет форму ответа ревьюера, ведёт историю раундов и считает метрики прогона. Каждый его модуль отвечает ровно за одно.

Обязательный минимум раундов означает, что план всегда проходит хотя бы один круг доработки. Если ревьюер одобрил его сразу, коуч получает план обратно с пометкой «замечаний нет — усиль слабые места». Испортить готовый результат этот круг не может: итогом считается последний одобренный раунд, а не последний по счёту (`finalRound`), и в `data/output.md` остаётся именно он. Исключение одно — `needs_human_professional`: предохранитель останавливает прогон немедленно и отменяет любое прежнее одобрение.

## Файлы

| Файл | Что это |
|---|---|
| `app/page.tsx` | Экран и всё его состояние: idle / running / result / error |
| `app/layout.tsx` | Root layout: шрифты, `globals.css` и скрипт, ставящий тему до отрисовки |
| `app/globals.css` | Tailwind v4 и переменные темы (светлая и тёмная) |
| `components/AgentForm.tsx` | Ввод задачи и запуск |
| `components/ReviewPanel.tsx` | Вердикт, score, история раундов, замечания, параметры прогона |
| `components/PlanView.tsx` | Рендер плана из markdown |
| `components/ThemeToggle.tsx` | Переключатель темы: система / светлая / тёмная |
| `components/ui/*.tsx` | Примитивы shadcn/ui |
| `lib/agent-result.ts` | Типы ответа API и оформление вердиктов |
| `lib/utils.ts` | `cn()` для shadcn |
| `app/api/agent/run/route.ts` | POST-эндпоинт, тонкая обёртка над harness |
| `src/agents/healthCoach.ts` | Агент-коуч: `createCoach(instructions)` |
| `src/agents/safetyReviewer.ts` | Агент-ревьюер: `createReviewer(instructions)` |
| `src/harness/runHealthAgent.ts` | Оркестратор: клиент DeepSeek и цикл ревью, `runHealthAgent(task, { minRounds?, maxRounds? })` |
| `src/harness/promptVersions.ts` | Загрузка промптов из `prompts/` и активные версии |
| `src/harness/validateReview.ts` | Zod-схема ревью, разбор JSON и ретрай |
| `src/harness/rounds.ts` | История раундов: `RoundState { round, plan, review }` |
| `src/harness/score.ts` | Выбор итогового раунда, его score и флаг `improved` |
| `prompts/healthCoach.v1.md` | Системный промпт коуча |
| `prompts/safetyReviewer.v1.md` | Системный промпт ревьюера |
| `index.ts` | Тонкий CLI-враппер над `runHealthAgent` |
| `data/profile.md` | Профиль: цели, ограничения, предпочтения. Правьте под себя |
| `data/log.md` | Дневник последних дней: еда, сон, тренировки |
| `data/output.md` | Последний одобренный план (перезаписывается) |

Никакой базы данных — только markdown.

`index.ts` не удалён, а превращён в CLI-враппер: логика в нём не дублируется (только разбор аргумента и код возврата), а быстрый прогон цикла из терминала остаётся — это дешёвый способ проверить агентов и предохранитель, не открывая браузер.

Кроме целевой структуры в проекте есть служебные файлы фреймворка: `app/layout.tsx` (App Router без root layout не работает), `tsconfig.json` и `next.config.ts` — последний нужен ровно для `agentRules: false`, иначе Next при каждом запуске генерирует в корне `AGENTS.md` и `CLAUDE.md`.

Модули `src/harness/*` и каталог `prompts/` добавлены по `spec3.md`: harness разбит на слои по ответственности, промпты вынесены из кода в версионируемые файлы.

Остальное сверх целевой структуры — слой представления на shadcn/ui: `components.json`, `postcss.config.mjs`, `app/globals.css`, `components/`, `lib/`. shadcn держит исходники компонентов в репозитории, поэтому без своих файлов не работает; решение и его границы описаны в разделе «Дополнение» в `spec2.md`. Логика агентов при этом не менялась.

## Дизайн

Тёплый монохром, плоские карточки с рамкой в 1px, без градиентов и теней. Цветных заливок крупных блоков нет.

Акцент — приглушённый травяной зелёный (`#3b6b52`, в тёмной теме `#7cb894`). Он появляется точечно: кнопка запуска, фокусный ринг, полоса score, номера шагов, маркеры списков в плане и квадрат у заголовка. Вердикты живут на своих пастельных парах — `approve` (зелёная), `revise` (янтарная), `needs_human_professional` (красная).

Тема переключается в правом верхнем углу: **как в системе / светлая / тёмная**. Выбор хранится в `localStorage`; в режиме «как в системе» страница слушает настройку ОС и переключается на лету. Без JavaScript тема всегда светлая.

Шрифты: Golos Text (интерфейс), Playfair Display (заголовки и структура плана), JetBrains Mono (числа и метаданные) — все с кириллицей.

Новый примитив shadcn добавляется так:

```bash
npx shadcn@latest add dialog
```

`shadcn init` запускать не нужно и нельзя: CLI неверно определяет проект как `next-pages` со `src/`-директорией. `components.json` уже лежит в корне и написан под фактическую структуру.

## Настройка под себя

Замените содержимое `data/profile.md` и `data/log.md` — формат свободный, агенты читают их как есть. Чем конкретнее ограничения (травмы, аллергии, непереносимости), тем строже ревьюер отсекает неподходящие рекомендации.

Поведение агентов меняется в промптах: `prompts/healthCoach.v1.md` (роль, формат плана, запреты) и `prompts/safetyReviewer.v1.md` (критерии, вердикты, шкала score).

Промпты версионируются файлами. Чтобы попробовать новую редакцию, не теряя старую: скопируйте `prompts/healthCoach.v1.md` в `prompts/healthCoach.v2.md`, поправьте текст и переключите версию в `ACTIVE_PROMPTS` (`src/harness/promptVersions.ts`):

```ts
export const ACTIVE_PROMPTS: PromptVersions = { coach: 'v2', reviewer: 'v1' };
```

Больше править нечего: версия попадает в ответ API (`promptVersions`) и видна в левой колонке — по ней ответ соотносится с промптом, на котором он получен. Возврат к прежнему поведению — обратное переключение.

## Ответы API

| Ситуация | Ответ |
|---|---|
| План одобрен | 200, `verdict=approve`, план записан в `data/output.md` |
| Медицинский запрос | 200, `verdict=needs_human_professional`, UI показывает предупреждение вместо плана |
| Не одобрен за 3 раунда | 200, `verdict=revise`, три записи в `rounds`, в `data/output.md` ничего не записано |
| Пустая задача | 400 |
| Нет ключа, ревьюер дважды вернул невалидный JSON | 500 с текстом ошибки |

Коды возврата CLI: `0` — план одобрен или запрос корректно отклонён как медицинский; `1` — нет аргумента, план не одобрен за 3 раунда или ошибка.
